# Core web/control server (required everywhere, dev machine included).
aiohttp>=3.9

# Synthetic test camera. Lightweight; lets the stream work without hardware.
Pillow>=10.0

# On the Raspberry Pi also install the hardware deps: requirements-pi.txt
# (the server falls back to mock backends when they are missing).
